{
  "name": "vnd-ceo-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "deploy-frontend": "npm run build --prefix frontend && firebase deploy --only hosting"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
